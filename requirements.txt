# Core deep learning
torch>=2.0.0
torchvision>=0.15.0

# Segmentation architectures & backbones
segmentation-models-pytorch>=0.3.3
timm>=0.9.0
monai>=1.3.0

# Image processing / augmentation
albumentations>=1.3.0
opencv-python-headless>=4.8.0
pillow>=10.0.0
scikit-image>=0.21.0

# Numerical / data / stats
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
scikit-learn>=1.3.0
pingouin>=0.5.3          # intraclass correlation coefficients (Experiment 6)

# Plotting
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# Optional: only needed for the histology-pretrained ViT encoders in Experiment 2a
# (Phikon-v2, UNI, CONCH, H-optimus-0, Virchow2). Some require gated HuggingFace access.
# huggingface-hub>=0.20.0
