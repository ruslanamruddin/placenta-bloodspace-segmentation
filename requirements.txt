# Dependencies for the placental blood-space segmentation pipeline.
#
# Lower bounds are the minimum versions known to expose the APIs this code uses
# (modern Albumentations transform signatures, torch.amp, weights_only loading).
# The pipeline was last verified end-to-end against the "Tested with" versions
# below; newer releases are expected to work.
#
# Tested with: torch 2.11, albumentations 2.0.8, segmentation-models-pytorch 0.5.0,
#              timm 1.0.27, pingouin 0.6.1, numpy 2.4, pandas 3.0, scipy 1.17,
#              opencv-python-headless 4.13, matplotlib 3.10.

# Core deep learning
torch>=2.4
torchvision>=0.19

# Segmentation architectures & backbones
segmentation-models-pytorch>=0.5.0
timm>=1.0.0

# Image processing / augmentation
albumentations>=2.0.0
opencv-python-headless>=4.8.0
pillow>=10.0.0

# Numerical / data / statistics
numpy>=1.26.0
pandas>=2.0.0
scipy>=1.10.0
pingouin>=0.5.4          # intraclass correlation coefficients (Experiment 6)

# Plotting
matplotlib>=3.7.0

# --- Optional ---
# Only required for the histology-pretrained ViT encoders in Experiment 2a
# (Phikon-v2, UNI, CONCH, H-optimus-0, Virchow2). Some weights are gated on
# HuggingFace and need `huggingface-cli login`. The encoder comparison skips
# any model that cannot be loaded, so these are not needed for the rest of the
# pipeline or for predict.py.
#   transformers>=4.40.0
#   huggingface-hub>=0.24.0
